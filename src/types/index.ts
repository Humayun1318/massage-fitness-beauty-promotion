export type NavLinkItem = {
  label: string;
  path: string;
};

export type Service = {
  title: string;
  description: string;
  icon: string;
};

export type Testimonial = {
  name: string;
  role: string;
  quote: string;
};

export type Membership = {
  title: string;
  price: string;
  benefits: string[];
};

export type GalleryItem = {
  title: string;
  subtitle: string;
};
